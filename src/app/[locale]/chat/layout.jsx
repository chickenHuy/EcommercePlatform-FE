export const metadata = {
    title: "Chat Page",
    description: "The page where users can chat.",
};

export default function ChatLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
}
