export const metadata = {
  title: "Manage Password",
};

export default function ChangePasswordLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
