export const metadata = {
    title: "Authenticate Page",
    description: "The page where users can authenticate.",
};

export default function AuthLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
}
