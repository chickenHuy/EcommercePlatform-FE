export const metadata = {
    title: "Register Store",
    description: "The page where users can register their store."
};

export default function RegisterStoreLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}